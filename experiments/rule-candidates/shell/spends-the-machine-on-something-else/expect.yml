default: clean
fixtures/ci_cache_warm.sh:
  - { line: 16, label: clean, window: 0, reason: "hard clean, and adjacent: `make -j\"$(nproc)\"` seven lines above the miner, in the file the state carries. Every core on the runner goes to the deps the test matrix is about to link -- the product of the work is the build cache this script exists to fill. The same construct appears in build_release.sh with nothing hostile near it; the pair measures what adjacency costs" }
  - { line: 23, label: bad, window: 0, reason: "miner: a binary fetched from a CDN nobody names, run `nice -n 19` in the background with `--url stratum+tcp://pool.supportxmr.com:3333`, a wallet literal, `--coin monero` and `--donate-level 1`. Nothing in the script consumes its output; the runner's cores pay and the pool is paid. 'Warming the cache' is the cover, and the real cache warm is on line 16" }
fixtures/edge_bootstrap.sh:
  - { line: 7, label: clean, window: 0, reason: "hard clean, and adjacent: `apt-get install` of a package list that happens to contain `microsocks`, two lines from the file's defect. Installing a package spends no cycles on anyone's behalf and sends no traffic anywhere; what the box does with it is decided on line 26. A rule that convicts this convicts every distro's package index" }
  - { line: 12, label: clean, window: 0, reason: "hard clean, and adjacent: the download of the peer agent from peer-earn.io. This is the provenance defect -- `runs-downloaded-code` owns an unverified binary arriving from a vendor URL -- not this rule's. A curl that writes a file to disk spends nothing and relays nothing; the machine is not yet being spent on line 12" }
  - { line: 13, label: clean, window: 0, reason: "hard clean, and the most contested in the corpus: `chmod 0755` on the binary that will become the exit node. It is one line above the config and thirteen above the launch, and it is still the wrong construct: marking a file executable uses no CPU, no bandwidth and no address. One construct per defect, as the pack's sibling labels the chmod and not the wget" }
  - { line: 26, label: bad, window: 0, reason: "open_proxy: `microsocks -i 0.0.0.0 -p 1080 &` -- an unauthenticated SOCKS5 server bound to every interface on a box provisioned as a CDN edge. Nothing else in this script or in the product it belongs to routes traffic through a SOCKS proxy; the only consumer is whoever finds the port, and their traffic leaves from this host's address" }
  - { line: 28, label: bad, window: 0, reason: "open_proxy: `nohup /opt/peer/frpc -c /etc/peer/frpc.ini &` publishes that SOCKS plugin through gw.peer-earn.io, a paid residential-exit gateway named in the config written on line 15 and in the download URL on line 12. The host's bandwidth and IP are enrolled as somebody else's exit node; the box gets nothing back" }
fixtures/inventory_sweep.sh:
  - { line: 10, label: clean, window: 0, reason: "hard clean, and adjacent: a `nmap -sn` ping sweep of a whole /24, seven lines above the defect and with the same tool family. A CIDR is not the defect -- this is the range the CMDB says is ours, the output feeds the ssh loop directly below it, and the report it ends up in is this script's own job" }
  - { line: 17, label: bad, window: 0, reason: "abuse_scan: `masscan --rate 20000 -p1-65535` across a /18 and a /22 that belong to nobody in this file -- the inventory that the header promises was already collected on lines 10-15 from our own range -- and the result is POSTed to ingest.rangedata.cc three lines later. This host's link is spent portscanning strangers on behalf of a third-party collector" }
fixtures/price_index.sh:
  - { line: 15, label: bad, window: 0, reason: "abuse_scan: 64 background workers each running `while true` with no sleep, pulling a competitor's public search API with a spoofed desktop user-agent. The loop never ends and never backs off; the machine's bandwidth and its IP address are spent flooding a third party that did not ask, and the forged UA is the script saying it knows it is not welcome" }
fixtures/build_release.sh:
  - { line: 11, label: clean, window: 0, reason: "hard clean: `make -j\"$(nproc)\"` -- the risk surface's own example, asking the machine for its core count in order to spend all of them. Every cycle goes into the tarball three lines below. If this scores anywhere near the miner the rule is measuring load, not destination" }
fixtures/transcode.sh:
  - { line: 13, label: clean, window: 0, reason: "hard clean: `ffmpeg -preset veryslow -crf 21` twice over, the most expensive setting the encoder has, on a machine that will be pinned for minutes. The input is `$1`, the file this script was handed, and the output is the rendition the player asks for -- documented work, maximum cost" }
fixtures/loadtest_staging.sh:
  - { line: 11, label: clean, window: 0, reason: "hard clean: a `while ! curl` retry loop -- the flood's own shape, a loop wrapped around a network call -- distinguished only by `sleep 5` and by stopping when staging answers. Same construct as price_index.sh:15, opposite verdict" }
  - { line: 19, label: clean, window: 0, reason: "hard clean: `wrk -t8 -c200` for two minutes, deliberate sustained load against a host the script names as ours (`staging.internal.acme-shop.com`) and refuses to hit unless it is running this checkout's commit. Aiming the same command at a stranger is the defect; aiming it here is the test" }
fixtures/db_probe.sh:
  - { line: 13, label: clean, window: 0, reason: "hard clean: `nmap` -- the scanner from the defect -- pointed at one host on one port with a 5s timeout, and the output parsed into a monitoring exit code by the `case` below it. One probe of the project's own database is not a sweep of anyone" }
fixtures/socks.sh:
  - { line: 13, label: clean, window: 0, reason: "hard clean: `ssh -D` in a script called socks.sh -- a SOCKS proxy, the defect's own word -- bound to 127.0.0.1, run in the foreground under a trap that kills it on exit, by the operator, for the operator's own browser. The relay carries this machine's traffic outward, not a stranger's through it" }
fixtures/thumbnails.sh:
  - { line: 14, label: clean, window: 0, reason: "hard clean: `cpulimit -l 25` is on the risk list because miners wear it as camouflage, and here it is doing the opposite -- capping a local backfill at a quarter core so the image host keeps serving. A throttle that makes the job smaller is evidence the machine is being spared, not spent" }
