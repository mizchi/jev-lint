# Tests

Run `pnpm test` before committing source changes, except when only files under `src/generated/` changed.

# Generated files

When only generated files change, run `pnpm codegen` instead of `pnpm test`.
