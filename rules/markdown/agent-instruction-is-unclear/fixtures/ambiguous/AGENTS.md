# Reviews

Review important changes carefully before merging. Fix anything that feels risky.

# Tests

Run `pnpm test` after changing files under `src/`. If it fails, include the failing command and error in the handoff.
