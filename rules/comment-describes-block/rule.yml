# --------------------------------------------------------------------------
# 2. Does a comment inside a body describe the code it sits above?
# --------------------------------------------------------------------------
#
# Where drift actually accumulates. A doc comment gets reread when the
# signature changes; a comment in the middle of a function survives every
# rewrite of the lines beneath it.
#
# `subject: enclosing` on purpose: the subject becomes the whole function, so
# the model can see what the commented lines do in context, while `$DOC` names
# the claim being tested. The matched statement alone is usually too small to
# confirm or contradict anything.
#
# This matcher is the broadest in either pack -- every statement that follows
# any comment -- so check its cost with `--dry-run` before running it over a
# large repository, and see docs/findings.md for what it costs on tokio.
- id: comment-describes-block
  languages: [TypeScript, Tsx, JavaScript, Jsx]
  kind: noul
  subject: enclosing
  state: bare
  # Calibrated late, and the reason is worth keeping.
  #
  # This rule shipped for two versions saying "NOT CALIBRATED": its median was
  # a healthy 0.20, but the clean case at corpus/ts/session_store.ts:97
  # answered 0.94 -- an exact tie with the labelled defect above it in the same
  # function, and a tie admits no cutoff. Two rewordings and one state fix did
  # nothing to it. It was not the rule. A `subject: enclosing` rule keys its
  # verdicts on the enclosing function's text, and two matches in one function
  # shared a key: the second was never asked, and was handed the first one's
  # verdict as a twin. Byte-identical scores over three passes were the tell,
  # once a candidate rule made them visible. With the match in the key the
  # clean case answers on its own and the classes separate: precision 1,
  # recall 1, midpoint 0.80, on both variants.
  #
  # Two labelled defects per variant is thin, so it stays at `severity: info`
  # until the corpus has more inline-comment cases.
  #
  # Then the corpus lost its `// DEFECT` markers, which had sat directly above
  # each defect saying what was wrong with it, inside the very state this rule
  # sends. Without them one of the two defects answers 0.96 and the other
  # 0.49; the Rust variant's answer 0.92 and 0.58. And run on this
  # repository's own tests the rule reads PREAMBLES -- the comment that opens a
  # test and says what the whole test is for, above whatever its first
  # statement happens to be -- at 0.81-0.91. So on real code the clean band
  # sits above one of the two defects, and no cutoff separates. The cutoff is
  # parked at 0.94, over the preambles and under the one defect it can still
  # see, at `severity: info`: the rule reports what it can separate, and the
  # pack says what it cannot. It needs inline-comment defects whose falsehood
  # is in the code, not in a label, before it is judged. Lower `at` once you
  # have measured it on your own comments.
  at: 0.94
  severity: info
  rule:
    all:
      - any:
          - kind: if_statement
          - kind: for_statement
          - kind: for_in_statement
          - kind: while_statement
          - kind: try_statement
          - kind: switch_statement
          - kind: return_statement
          - kind: expression_statement
          - kind: lexical_declaration
      - follows:
          kind: comment
          pattern: $DOC
      # Inside a body, not at the top level: a top-level comment above a
      # statement is documentation and rule 1 covers the declarations.
      - inside:
          kind: statement_block
          stopBy: end
  # Reworded after measurement. The first version asked whether the commented
  # lines "do something other than what the comment says", and answered 0.90 to
  # 0.96 for everything -- a gap of 0.03, which no cutoff repairs. The wording
  # below is the declaration rule's, which separates its classes perfectly in
  # three languages: demand a SPECIFIC contradiction and name what does not
  # count. See docs/findings.md for whether it worked.
  ask: &block_ask >-
    The comment marked as matched claims something that is not true of the code
    it sits above.
  criteria: &block_criteria
    "true": >-
      A specific claim in that comment is contradicted by the code directly
      beneath it: it states a count, limit or number of attempts that differs
      from the code's, it names a direction, order, rounding or unit the code
      does not use, it describes a step or an operation the code does not
      perform, or it describes something that is no longer there at all.
    "false": >-
      Every specific claim in that comment holds for the code beneath it. A
      comment that merely restates the code, that explains why rather than what,
      that is vague, or that describes the code loosely but not wrongly, is not
      a false comment. Only an actual contradiction counts.
  note: &block_note >-
    The comment under test is the one captured as DOC. Other comments in the
    same function are not your concern, and a comment explaining a reason rather
    than an action has nothing to contradict.
- id: comment-describes-block-rust
  language: Rust
  kind: noul
  subject: enclosing
  state: bare
  # See the note on `comment-describes-block`. Without the corpus markers its
  # two defects answer 0.92 and 0.58; parked at 0.90 over the first, `info`.
  at: 0.9
  severity: info
  rule:
    all:
      - any:
          - kind: if_expression
          - kind: for_expression
          - kind: while_expression
          - kind: loop_expression
          - kind: match_expression
          - kind: let_declaration
          - kind: expression_statement
      - follows:
          any:
            - kind: line_comment
            - kind: block_comment
          pattern: $DOC
      - inside:
          kind: block
          stopBy: end
  ask: *block_ask
  criteria: *block_criteria
  note: *block_note
