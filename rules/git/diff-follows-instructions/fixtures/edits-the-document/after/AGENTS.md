# Contributing

## Dependencies

- This project ships no runtime dependencies. `package.json`'s
  `dependencies` field stays empty; anything needed at runtime is written
  here or comes from the standard library.

## Style

- Two-space indentation.
