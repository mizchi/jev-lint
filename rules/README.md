# The shipped rules

One directory per rule (family): `rule.yml` holds every language variant, `evals/` holds the cases that prove it, the labels, and the accepted baseline. `jev-lint eval` runs them. The notes below were the headers of the packs these rules shipped in before the split; the measurements they cite are recorded in `docs/`.

## naming



## comments



## guarantees



## tests



## messages



## config


