# The config for working on the MoonBit rules in this repository, named
# explicitly: `jev-lint eval rules/moonbit --config docs/moonbit.jev-lint.yaml`.
# It is not the repository's own config -- two in one directory is an error,
# and jev-lint's own source has no MoonBit in it.
#
# The parser is not in the repository: it is a 1.2 MB platform-specific
# library. Build it once with
#
#   npm run parsers:moonbit
#
# which clones moonbitlang/tree-sitter-moonbit and writes
# .jev-lint/parsers/moonbit.dylib (gitignored).
languages:
  moonbit:
    libraryPath: ../.jev-lint/parsers/moonbit.dylib
    extensions: [mbt]
    expandoChar: _
rules:
  catch-hides-failure: on
  comment-describes-block: on
  comment-describes-declaration: on
  doc-errors-match-body: on
  error-message-matches-condition: on
  fn-name-promises: on
  idempotent-name: on
  log-level-matches-event: on
  module-name-describes-contents: on
  module-naming-consistent: on
  pure-name-is-pure: on
  safe-name-is-safe: on
  snapshot-only-behaviour-claim: on
  test-name-describes-code: on
  test-name-verifies-claim: on
  tests-cover-failure-paths: on
  type-name-describes-shape: on
  var-name-describes-value: on
